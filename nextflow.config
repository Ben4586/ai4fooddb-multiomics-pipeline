process {
    executor = 'local'
    cpus = 4
    memory = '6 GB'

    withName: process_micro {
        cpus = 1
    }

    withName: simulate_metabolites {
        cpus = 1
    }
}

executor {
    queueSize = 10
}

process {
    container = 'ai4fooddb'
}

docker {
    enabled = true
}